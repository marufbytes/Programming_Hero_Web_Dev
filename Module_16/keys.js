const computer={
    brand:"Lenovo",
    price:5000,
    processor:"Inter",
    hdd: "512gb",
    monitor: "hp"
}

const keys = Object.keys(computer);
console.log(keys);

const values = Object.values(computer);
console.log(values);